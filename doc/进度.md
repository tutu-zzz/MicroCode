# MicroCode 开发进度

## 当前状态

- M0 — Python 工程骨架：已完成。
- M1 — 领域类型与事件模型：已完成（2026-08-12）。
- 下一阶段：M2 — JSONL Journal 与 Artifact Store。

## M1 交付

- 定义 27 个 MVP 事件名称。
- 实现不可变、禁止额外字段、强制 UTC 时间的 `EventEnvelope`。
- 实现不接受 `Path`、任意 Python 对象、非字符串键和非有限浮点数的严格递归 JSON 校验。
- 实现显式接收 `stream_version` 的事件工厂，不在 Journal 之外猜测版本。
- 实现 `PayloadRegistry` 及 `session.started`、`user.message_recorded` payload schema。
- 支持未知事件的严格拒绝与只读诊断模式。
- 增加序列化、UTC、不可变性、非法对象和 payload schema 测试。

## 验收结果

```text
pytest: 14 passed
ruff check: passed
ruff format --check: passed
mypy: passed (52 source files)
infrastructure document check: passed
```
